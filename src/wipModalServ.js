(function(){
	
	angular.module('wipModal', ['ui.bootstrap'])
		.factory('wipStashServ', wipStashServ);

		function wipStashServ () {
			return {
				create: create,
				remove: remove,
				get: get
			};

			function create (argument) {
				
			}

			function remove (argument) {
				
			}

			function get (argument) {
				
			}
		}

}());